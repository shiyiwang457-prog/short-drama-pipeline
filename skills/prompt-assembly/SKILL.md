---
name: prompt-assembly
description: 提示词装配台——把已定稿的分镜表装配成 AI 视频平台能直接跑的提示词文本。四站流水线（模型档案→逐镜装配→生成回看→回测登记），两道硬门禁（平台参数已填 / 画面描述段负向词命中数为 0）。装配材料全部来自本地规则库（~/Desktop/11/_规则库/：模型档案、正向改写表、措辞库、部件清单）。触发："写提示词""生成提示词""装配提示词""这一镜的提示词""分镜转提示词""提示词表"。⚠️ 只做装配层，不引入分镜表没有的构图判断（那是 storyboard 的事）；也不改台本。
---

# 提示词装配台 (prompt-assembly)

## 定位

**上游**：`storyboard` 出的分镜表（构图与表演判断已定）
**本层**：把分镜表**翻译**成平台文本
**下游**：平台生成 → 回看 → 回填实测

```
story-doctor  →  storyboard  →  【prompt-assembly】  →  平台生成
   台本            分镜表           提示词文本
```

---

## 三条元规则（每次装配前先读一遍）

### 1 · 本层只翻译，不判断

**分镜表没写的构图判断，这里不许发明。**

若装配时发现分镜表缺了某项（焦距没写、切点类型没定、静止清单为空），
**回上游补分镜表，不在提示词里补。**

> 实证（《纪念日》2026-08-13）：构图决策被直接写进了提示词 v3，分镜表还停在"固定·标准"×11。
> **后果不只是不同步——那些决策绕过了导演审阅。**

### 2 · 禁令不许直接下发

**任何以「不／没有／禁止／不许」开头的画面要求，必须先过 [_正向改写表.md](~/Desktop/11/_规则库/_正向改写表.md)。**

> Seedance 官方规则：**只告诉它你要什么，别告诉它你不要什么**——写了负向词，模型忽略。
> 更坏的可能：扩散模型把被否定的名词当成要画的东西，**「不要抓衣角」被读成「抓衣角」**。

### 3 · 平台参数是项目级的，不是 skill 级的

**skill 不写死秒数、不写死画幅。** 开工前去项目的模型档案／全片约束表拿参数。
**换平台就换数字，换不了流程。**

---

## 材料接口

**位置**：`~/Desktop/11/_规则库/`

| 要什么 | 去哪 | 性质 |
|---|---|---|
| **平台能力与语法** | `_模型档案-<平台>.md` | 厂商文档 ＋ 实测（官A/官B/实C/推D） |
| **禁令 → 正向句** | `_正向改写表.md` ⭐ | 由卡推导 ＋ 平台规则 |
| **判断 → 平台认得的字** | `_措辞库.md` | 官方词表（平A）＋ 卡蒸馏（书B） |
| **部件有哪些、可不可见** | `部件清单.md` | 六系：气/声/形/关节功能/机体觉/特写级 |
| 某张卡的原文 | `_索引/<站名>.md` → Read 全卡 | 书证 |

⚠️ **书证卡的 A/B/C/D 与模型档案的 官A/官B/实C/推D 是两套分级，不通用。**

---

## 四站流水线

### 🚪 站 0 · 模型档案（**硬门禁**）

**开工前必须拿到平台的四个参数，填进项目的模型档案或全片约束表。**

| # | 参数 | 怎么拿 | 影响 |
|---|---|---|---|
| 1 | **单块时长下限** | 界面时长控件的最小值 | ⭐ **通常比上限要紧**——它会逼着改设计 |
| 2 | 单块时长上限 | 最大值 | 哪些场要多切一刀 |
| 3 | **画幅默认值** | 比例选项 | ⚠️ 与项目画幅不符 → **每条提示词都要显式改** |
| 4 | 有没有负向词输入框 | 看界面 | 有＝平台自己加的，**效力要单独验** |

**门禁判据**：四项都有值，否则不许进站 1。

#### ⭐ 站 0 的第二件事：选装配模板分支

**官方语法 ≠ 第三方包装层支持的语法。** 必须先确认包装层认不认这四样：

| 语法 | 怎么验 | 认 | 不认 |
|---|---|---|---|
| **时间轴分段** | 写 `0-4秒／4-8秒`，看是否按段执行 | 走 **模板 A** | 走 **模板 B** |
| **四括号** `（）<> {} 【】` | 一条里放全，看四类是否各自生效 | 同上 | 退回自由文本描述 |
| `@图片N` 引用 | 传参考图并 @ 标注 | 用 | 靠文字描述锁一致性 |
| `--ratio` 等行内参数 | 末尾加参数看输出 | 用 | 走界面控件 |

⚠️ **没验之前，默认走模板 A（官方语法），但在提示词表里标「待验」。**

---

### 站 1 · 逐镜装配

#### 1.1 ⭐ 分镜表 → 提示词的映射表

**这是本 skill 的核心。一栏一栏搬，不许漏、不许加。**

| 分镜表的栏 | 装进提示词的哪一段 | 经过哪张表 |
|---|---|---|
| **轨道**（正常/DV/取景框） | **视觉风格** | 措辞库 · 画质 |
| **地点 ＋ 时间** | **场景环境** | — |
| 画面 | **主体 ＋ 场景环境** | — |
| **景别 ＋ 机位 ＋ 焦距** | **镜头运动/切换** | ⭐ 措辞库 §二/三/四（**只用模型认得的词**） |
| **可见状态（G2）** | **动作/事件** | ⭐ 部件清单 → 措辞库 §五张力档 |
| 光 | 视觉风格 | 措辞库 §六光位 |
| **镜级要求（禁令）** | 并入动作段 | 🔴 **必过正向改写表** |
| **台词** | `{ }` | 措辞库 · 对白格式 |
| 音频 | `（）` `< >` | — |
| **切点类型** | 决定**时间轴怎么分段** | — |

#### 1.2 装配顺序（六段式）

```
主体 + 动作/事件 + 场景环境 + 视觉风格 + 镜头运动/切换 + 音频
```

**只有「主体 ＋ 动作」是必填**，其余用来收紧结果。

**多镜块用时间轴**：

```
0-3秒：[开场画面、运镜、动作]
3-6秒：[中段发展]
6-10秒：[高潮或关键动作]
```

三条格式硬规：**用整秒 · 时间线不留空隙 · 一段一个主动作**。
⚠️ **时间戳是「时间预算」不是逐帧剪辑点**，允许小幅漂移。

#### 1.3 ⭐ 静止清单的上限

分镜表的静止清单可能列了七八项。**不许全部下发。**

```
每镜静止部件 ≤ 3–4 项
```

**挑最容易被模型搞坏的那几个，其余交给表演。**

> 🔴 **理由**：正向的静止清单比负向的更危险——**因为它可能真的生效**。
> 「肩保持不动、眉保持不动、手保持不动、头保持不动、呼吸保持不变」全部生效 ＝ **一张死脸**。
>
> 同源实证（EDIT-02 勘误）：从「光影动了脸就必须不动」推出全脸静止，
> **对生成模型是过度绝对化，会得到蜡像**。**禁的是重复，不是不动。**

#### 1.4 🚪 站 1 硬门禁：负向词清零

**提示词成稿后必须跑**（⭐ **不许只凭印象声称通过**）：

```python
import re
blocks = re.findall(r'```\n(.*?)```', 提示词表, re.S)   # 只取提示词正文
for b in blocks:
    desc = re.sub(r'\{[^}]*\}', '', b)                  # ⭐ 剔除 {} 内的台词
    hits = re.findall(r'不要|不许|不得|没有|禁止', desc)
```

**画面描述段内命中数必须为 0。**

⚠️ **两条判据边界**：
1. **`{ }` 内的台词不计**——人物说「不要。」是**台词**，不是给模型的负向指令
2. 校验点／说明文字不计——**门禁只管下发给模型的那部分**

> 🔴 **实证（2026-08-14，装配《纪念日》26 块时）**：
> 我在表尾写了「负向词 0 ✅」——**那是没跑就写的**。真跑出来 **11 处命中**。
> 其中 1 处是台词误报，**其余 10 处是真的**：
>
> | 我写的 | 改成 |
> |---|---|
> | 「那一块区域保持平整，**没有**形体变化」×3 | 「那一块区域**是平整的一片**」 |
> | 「他开口时前面**没有**间隔」 | 「他的话**紧接在**她的话之后」 |
> | 「她用**没有**力气的语气说」 | 「她用**低而慢**的语气说」 |
> | 「光平而均匀，几乎**没有**阴影」 | 「光平而均匀，**物体的投影极淡**」 |
> | 「句与句之间**没有**空隙」 | 「四句紧接着说，**一句落下另一句就起**」 |
> | 「他**没有**发出声音」 | 「只有嘴唇在动，**喉部保持静止**」 |
> | 「焦点散开，**没有**落在任何东西上」 | 「焦点散开，**视线越过屏幕落向远处**」 |
> | 「动作慢而连续，中间**没有**停顿」 | 「动作慢而连续，**一次落到底**」 |
>
> ⭐ **「没有」是最容易漏的一个**——它读起来像描述，实际是否定。
> **10 处里 8 处是「没有」。**

> 🔴 **实证（2026-08-14）**：《纪念日》两份旧提示词表，负向词命中 **52 处**与 **34 处**。
> 块头风格前缀每块 9 条负向，场 2 镜 6 单镜 11 条。**规模是全局的，不是个例。**

#### 1.4b 🚪 站 1 硬门禁：时间轴格式

**成稿后必须跑**：

```python
N = r'\d+(?:\.\d+)?'
segs = [(float(a), float(b)) for a, b in re.findall(rf'\n({N})-({N})秒：', block)]
整秒 = all(float(v).is_integer() for p in segs for v in p)
无缝 = all(abs(segs[i][1] - segs[i+1][0]) < 1e-9 for i in range(len(segs)-1))
对齐 = abs(segs[-1][1] - 块时长) < 0.01
```

**三项全 True 才算过。** 另加一条：**块时长本身也必须是整秒**——平台时长控件按整数秒取值，`13.5″` 这种块选不出来。

⚠️ **段长 < 1 秒的内容不做成时间轴段**，写进段内文字（「先…停半拍…然后…」）。
⚠️ **整块的画面变化都细于 1 秒时**（如 4 秒 6 变），**取消时间轴**，改「依次发生 N 个画面，每个长度相同」。

> 🔴 **实证（2026-08-14，《纪念日》v1→v2）**：v1 有 **19 块**用了 `1.2-2.5秒` 这类非整秒写法，
> **5 块的块时长本身带小数**（5.5／5.5／7.5／4.5／13.5）。
> 官方硬规第 1 条明写「用整秒，不用半秒」——**v1 当时没有这道门禁，所以没人拦住它**。

#### 1.4c 🚪 站 1 硬门禁：静止清单 ≤4 项（§1.3 的可执行判据）

```python
ST = r'(?<!镜)(?<!画面)(?<!构图)(?:头|颈|肩|躯干|背|腰|臂|手|指|脚|腿|膝|重心|面部|脸|眉|眼|嘴|下颌|喉|呼吸|姿态|全身|身体|视线|口型)[^，。：]{0,8}?(?:保持|停在|停住|不动|不变|垂在|落在)'
# 按【时间轴段】分别计数，不是按块
```

**每段命中 ≤ 4。**

⚠️ **两个必须排除的假阳性**：
1. **「镜头保持不动」「画面保持」「音量保持」**——主语不是人体
2. **「视线先落在 X，随后移开」**——那是**移动**描述，不是静止指令。写成「视线起初在 X」可避开

#### 1.4d 🚪 站 1 硬门禁：人物入画交代

**平台官方明令：禁止硬切、禁止对象凭空出现。**

**每块里人物第一次出现的那一段，必须满足其一**：

| 情形 | 要求 |
|---|---|
| **块的第一段** | 人已在场 —— **必须写他在画面的哪个位置、什么姿态** |
| **块的中途** | 🔴 **必须写他怎么进来的**：走进／伸进／镜头摇过去／镜头推进／从遮挡后露出 |

```python
ENTER = r'进入|走进|走向|走到|伸进|进画|俯身下来|从画面[左右上下]|转过来|转向|摇向|摇过来|推进|露出|站起来'
# 首次出现人物的段：i==0（已在场，查站位）或 re.search(ENTER, seg)
```

⚠️ **摄影机换手／视角转移要有物理证据**，并且**每次都要有，不能只给第一次**。

> 🔴 **实证（2026-08-14，《纪念日》场 1）**：
> 台本原文交代得好好的——「**镜头转过来，对准他**」「**他走进画面**」。
> 分镜 v2 加了「DV 换手」的新结构，**装配时只搬了动作，丢了「镜头怎么找到人」那一步**：
>
> ```
> 0-2秒：手持镜头追着浪跑。          ← 画面里只有浪
> 2-4秒：她把摄像机塞进他手里。       ← 🔴 两个人同时凭空出现
> ```
>
> 更隐蔽的一处：**第一次换手写了「抖动＋短暂失焦＝机器换手的物理证据」，
> 第二次换手（10-11 秒）却只写「摄像机换回她手里」——同一个动作，证据不对称。**
>
> ⭐ **教训**：**上游改结构时，被改动的那一段的"进出场逻辑"要重新走一遍**，
> 不能假设它跟着动作一起继承了。

#### 1.4e 🚪 站 1 硬门禁：音频不得为空 ＋ 跨块延续必须登记

**判据一**：**每块至少有一条 `< >`**。

```python
assert re.findall(r'<[^>]+>', block), "音频为 0"
```

⚠️ **括号缺席法只对「不想要的东西」有效**（配乐、字幕）。
**环境底噪是每块都该有的**——漏写不是"没有音乐"，是**一段没有声音的画面**。

**判据二**：分镜表里每一条 `< >` 都要能在提示词里找到对应。

> 🔴 **实证（2026-08-14《纪念日》）**：分镜表 16 条音频标记，**装配时漏搬 4 条**：
> `<白天房间的安静底噪>`（只搬到块 B）／ `<块A：清晨的风、脚步、呼吸>`（全丢）／
> `<段1-3：病房底噪 ＋ 规律心跳提示音>`（全丢）／ `<段4：提示音变调>`（全丢）。
> **另有 2 块零底噪。** 与运镜那次是同一个病：**映射表某一栏整栏没搬。**

#### 1.4f 🚪 必产出物：**后期接续清单**

**装配完成时必须同时产出一份「平台做不到、必须后期做」的清单。**

**三类必查**：

| 类 | 怎么找 | 为什么平台做不到 |
|---|---|---|
| **跨块声音延续** | 分镜表里搜「承接／延续／提前进入／一直到」 | 🔴 **N 块 ＝ N 条独立视频，声音的音色、节奏、相位不可能对上** |
| **所有块间接缝** | 块数 − 1 | 平台禁块内硬切，**每个切点都在块之间** |
| **画质／视角切换** | 轨道栏发生变化处 | 同上 |

⭐ **跨块声音的正确做法是两条腿**：
两块的提示词**仍然各自写那个声音**（给后期一个对齐参考，也让画面节奏对得上），
但**成片用后期单独铺的那一条**，盖掉两块各自生成的。

⚠️ **还有一条容易漏**：N 块各自生成的**环境底噪音色不会一致**。
同一空间的多块**建议后期铺一条统一底噪**，否则每次切场都能听出接缝。

> 🔴 **实证（2026-08-14）**：《纪念日》提示词表 v1/v2 **完全没有这一栏**。
> 分镜表明写「心跳仪声延续到场 7」「长鸣延续到场 8 的硬切」，
> 但两块是分别生成的——**这两条设计写了，却没有任何人负责实现它。**
> ⭐ **教训：装配层要能说出「这条我做不到，归后期」，而不是默默装配完就算交付。**

#### 1.4g 🚪 必产出物：**知识来源标注**

装配完成时必须清点：**哪些参数指得到卡，哪些是工程判断。**

⭐ **两类知识的性质不同，补法也不同**：

| 类 | 例 | 靠什么 | 为什么 |
|---|---|---|---|
| **要人判断的** | 表演对不对／节奏狠不狠／构图在说什么 | **靠书** | **机器跑一遍也不知道答案** |
| **可实测的物理量** | 光比／曝光档数／焦段／虚化度／方位角 | ⭐ **靠跑** | 生成一次就能反推模型认不认，**读十本书不如跑一条** |

**动作**：
1. 表里单列一节，把无书证的参数**按类连同用量一起列出**（「曝光档数 52 处 · 0 张卡」）
2. 每一类写明**实测后回填到哪**
3. 🔴 **标出最大的那处押注**——若实测证明模型读不懂这个词，多少处会作废

> 🔴 **实证（2026-08-14《纪念日》）**：为对齐一份外部提示词范例，全表加了
> **光源方位角 19 处 ＋ 曝光档数 52 处 ＋ 光比 38 处**，
> 事后清点发现**规则库对这三类是 0 张卡**——18 本书里**没有一本摄影用光、没有一本声音设计**，
> 唯一的两本分镜书早已扫完，光的卡只有 4 张且全讲「光在叙事上做什么」。
>
> ⭐ **结论不是"去补书"**：这三类是可实测的物理量，**跑一条比读十本准**。
> **但必须标注**，否则三个月后没人分得清哪些是书上的、哪些是当时编的。

---

#### ⭐ 门禁的元教训：**先自证你的统计脚本**

> 🔴 **实证（2026-08-14 同一次装配，我连错三次，全是同一个病）**：
>
> | 错 | 假结论 | 真相 |
> |---|---|---|
> | 正则只写 `\d+-\d+秒`（不含小数） | 「21/25 块时间没控住」 | **20/25 块有轴且合格** |
> | 运镜正则只放复合词（无裸「推/拉/摇/手持/固定」） | 「运镜只有 5 处，几乎全掉」 | **50 处，比分镜表还多** |
> | 静止清单正则未排除「**镜**头保持」 | 「场 16A 有 13 项」 | **5 项** |
> | 标题正则 `[0-9️⃣]*` 漏了 `🔟` | 「25 块」 | **26 块**（场 8 一直没被统计到） |
>
> **三条都把"我的正则太窄"读成了"稿子有问题"，并据此下了返工指令。**
>
> **做法**：任何用于门禁判定的正则，**先在一个已知答案的小样本上验一次**——
> 数出来的总数对不对得上人工清点？**对不上就是脚本错，不是稿子错。**
>
> ⭐ **最省的自证**：`块数` 这一个数字。**脚本数出的块数 ≠ 表里声明的块数 ＝ 脚本有洞**，
> 此时它报的一切数字都不可信。

#### 1.5 括号缺席法（最省的一类"否定"）

| 不想要 | 做法 |
|---|---|
| BGM／配乐 | **不写 `（ ）`** |
| 字幕／文字叠加 | **不写 `【 】`** |
| 环境音 | **不写 `< >`** |
| 台词 | **不写 `{ }`** |

**没写就是没有。** 不依赖平台读不读负向词。

---

### 站 2 · 生成回看

**出片后逐项对照分镜表**，不是"看着还行"。

| 查什么 | 判据 |
|---|---|
| **切点** | 时间轴写的分段，画面是否真的按段变化 |
| **景别／焦距** | 长焦那镜背景是否真的被压扁虚化 |
| **静止清单** | 该不动的部件有没有动 |
| **跨镜一致** | 逐字复用的那几段描述，出来是不是同一个画面 |
| **禁令** | 套路动作有没有出现（**出现＝失败确凿；没出现≠禁令生效**） |
| 音频 | 人声／音效／音乐是否按括号各自生效 |

⚠️ **负向类判据只有一个方向可靠**。要真测负向效力，需跑**不带该约束的同内容对照**。

---

### 站 3 · 回测登记

**把生成结果回填三处**：

| 结果 | 回填目标 |
|---|---|
| 平台能力／语法是否生效 | `_模型档案-<平台>.md` 的复核清单 |
| 禁令走"框"还是走"正向改写" | `_正向改写表.md` 表头验证状态 |
| 措辞是否被服从 | `_措辞库.md` §验证状态 |
| **单条规则的服从度** | ⭐ **各书证卡的 `实测` 字段**：`服从 / 部分 / 无效` |

**格式**：

```yaml
实测: "2026-08-14 · 块甲段3 · 服从 — 三次速度一致，无加速"
实测: "2026-08-14 · 块丙 · 部分 — 手停住了，但指节没有发白"
```

---

## ⚠️ 格式命名（2026-08-20 定 · 不再用编号）

**编号会和文件版本号撞车**（《纪念日》提示词表-v7 是**文件**的第 7 版，它用的格式是下面的「长块格式」）。
实证：PM 说「要 v7 格式」，被误解成内部格式编号 v4/v6，白绕一轮。**从此格式按功能叫名字：**

| 叫什么 | 长什么样 | 什么时候用 | 旧编号 |
|---|---|---|---|
| ⭐ **长块格式** | 五栏分栏（景别/构图机位/空间/角色状态/光影控制）＋ 成品控制词（【镜头N：景别 X秒】配额制、【镜头视角】【光与脸】、运动清单） | **文生视频**：一条文字直接生成十几秒多镜头长块；要「给人审＋给模型吃」双层文档 | v4 |
| **关键帧格式** | 一段剧情一个纯文本框（参考图行＋全局头＋`c01,4s,(空间:…)` 镜头串） | **图生视频**：先出关键帧图再让图动 | v6 |
| 内容自查清单 | 主体／动作+情绪／一直在动的／声音 | 写上面任一种时逐项核内容有没有盖住 | v5 |

**PM 说「v7」＝ 长块格式**（《纪念日》那份的样子）。**门禁脚本只认长块格式**，关键帧格式目前手工核。

## ⭐⭐ 唯一交付格式（2026-08-20 PM 定稿 · 照抄下面这份范例的样子出）

**PM 说「v7 格式」「我们那版」「我要的格式」，指的都是它。**
来源：PM 在另一工具的窗口里用 `script-to-video-prompt` 迭代 16 轮定稿，本 skill 照收。
**长块格式（旧 v4，《纪念日》那种五栏文档）已降为存档**，只在维护《纪念日》旧文件时使用。

### 🔴 标准范例（PM 原文 · 一字不改 · 每次出块前照着它对样子）

```text
场景={{Image 场景参考图}} 女主={{Image 女主参考图}} 男主={{Image 男主参考图}} 人物站位={{Image 站位参考图}}

真人电影风格,画面不要出现任何文字，生成视频无BGM；16:9，全程为手持DV拍摄形成的第一人称主观视角，生成画面本身就是DV拍摄素材，绝不切换至第三人称外部机位，不出现摄影者的脸或身体，不出现DV机器外壳、取景框、时间码、录制标志及任何文字。消费级DV真实成像质感，轻微手持呼吸晃动、自动对焦呼吸、柔和电子噪点和自然高光溢出；岩井俊二式日系青春电影氛围，夏日郊外，浅青奶油色调，低饱和、低对比、柔和侧逆光、空气薄雾与青涩治愈感。

c01,4s,(空间:夏日郊外小路)(姿态:男主-正面站立看向DV镜头)(位置:男主-画面中央距离镜头约两米)(情绪:男主-温柔放松 6/10) 中景，画面完全采用女主手持DV拍摄男主的第一人称视角。男主安静地看着镜头，阳光从他的侧后方穿过发丝，形成柔和透明的轮廓光。他先微微垂下眼睛，随后重新看向镜头，嘴角极其自然地扬起，露出克制而温柔的笑容。短暂停顿后，他向镜头伸出一只手，手掌由画面下方逐渐靠近，轻微遮住镜头，焦点从他的脸自然转移至指尖。

c02,3s,(空间:夏日郊外小路)(姿态:男主-伸手接过DV)(位置:男主-紧贴镜头前方)(情绪:男主-轻松亲昵 7/10) 特写，男主的手掌占据大部分画面，手指触碰DV机身，传来轻微的塑料外壳摩擦声。女主松手，男主平稳接过DV；镜头在手掌遮挡下发生一次连续、真实的接机晃动，随后被男主向下压并转动约180度。转动过程中曝光和焦点短暂漂移，画面不能中断、不能黑场、不能切换外部机位。女主趁镜头被遮挡时自然转过身，朝小路前方缓慢走去。

c03,3s,(空间:夏日郊外小路)(姿态:女主-背对镜头缓慢迈步)(位置:女主-镜头前方约一米)(情绪:女主-轻盈羞涩 6/10) 脚部特写，镜头转动完成后低机位对准女主的双脚，从背后跟拍她缓慢迈出的脚步。鞋底轻轻擦过路面，脚边草叶被夏日微风吹动，衣摆偶尔进入画面上缘。男主持DV的动作略显生涩，画面带有极其克制的手持微晃和轻微自动对焦呼吸，保持真实生活录像感。

c04,5s,(空间:夏日郊外小路)(姿态:女主-背对镜头缓慢向前行走)(位置:女主-从镜头前方一米逐渐走到三米处)(情绪:女主-放松温柔 7/10) 镜头从女主脚部开始持续而缓慢地向上抬升，依次掠过小腿、衣摆、腰背和被风吹动的发丝，最终形成女主背影全景。她沿着夏日小路慢慢向前走，身体松弛，衣角和头发随风轻摆；柔和逆光包裹她的轮廓，远处树影与浅青色天空轻微过曝，背景呈现柔软通透的空气感。镜头在完整背影上停留片刻，只保留微风、树叶摩擦、脚步声、衣料声和DV机械运转底噪，不加入台词或任何音乐。【不出现任何文字字幕】
```

### 结构拆解（照这三层写）

| 层 | 内容 |
|---|---|
| **第 1 行** | 参考图绑定，只列本段用到的：`场景={{...}} 女主={{...}} 男主={{...}} 人物站位={{...}}` |
| **第 2 段** | 全局头，**一整个自然段不换行**：`真人电影风格,画面不要出现任何文字，生成视频无BGM；16:9，` ＋ 视角机位声明 ＋ 画质 ＋ 风格色调 ＋ 本段专属约束 |
| **镜头串** | `cNN,Ns,(空间:…)(姿态:角色-动作)(位置:主体-画面位置)(情绪:角色-情绪词 N/10) ` ＋ 景别 ＋ 连续动作（拐点分明、部件级） ＋ 台词内嵌；**镜间空一行** |
| **末镜收尾** | `只保留<音效白名单>，不加入台词或任何音乐。【不出现任何文字字幕】` |

### 硬约定（从 16 轮迭代里抽的，逐条守）

| # | 约定 |
|---|---|
| 1 | **一段剧情 ＝ 一个纯文本框**；多段剧情 ＝ 多个框。窗口显示，**不落文件** |
| 2 | 四括号顺序固定：**(空间)(姿态)(位置)(情绪)**；多主体在同一括号内用 `; ` 分隔 |
| 3 | 情绪必须打分：`情绪词 N/10` |
| 4 | 时长写在镜号后 `c01,4s`；**平台单块下限 4 秒** |
| 5 | 台词内嵌正文：`<角色>用中文<情绪><语气>地说道<台词>`；画外的标「在画外」 |
| 6 | 心声：标「非画面内心独白」＋**嘴唇保持闭合、绝不产生口型** |
| 7 | 跨镜同一动作写「与上一镜进行动作匹配／同一批药片」**防重复执行** |
| 8 | 同机位续拍写「**同一机位连续拍摄，不切换景别**」 |
| 9 | ⭐ **没有的内容整行跳过**——没台词就没有台词句，不发明 |
| 10 | 🔴 **护栏负向句是格式固定件**（无BGM／不出现文字／不能黑场／不切换外部机位）——**负向词门禁对它们豁免**；但**动作正文仍以正向优先**，音效用「只保留…」白名单式 |


### 🚪 现实逻辑五问（出块后、跑 check_shots 前，逐问过一遍）

**格式门禁查不了逻辑。** 实证（2026-08-20 森林三场）：门禁全绿时成品里躺着两处硬伤，
是 PM 开口才查出来的——从此这一步是流程，不等人开口。

| # | 问 | 实证（当天抓到的） |
|---|---|---|
| 1 | **视角物理**：POV／手持镜的每次镜头运动，答得出「摄影者的身体此刻在做什么」吗？剧情里摄影者停了，镜头就不许跟拍 | 「跟在身后拍摄」×「发现女孩没跟上」互相矛盾 |
| 2 | **跨块连续**：同一人物在相邻块的位置/姿态/持物衔接了吗？衔接要显式写（「男孩仍停在一米半处」） | 场1结尾男孩在1.5米，场2开头跳回3米 |
| 3 | **言行一致**：台词的言语行为和身体动作配吗？喊「冲」的人不能站着不动 | 站定喊「冲啊——驾」 |
| 4 | **入画有因**：任何身体部位入画都给了物理理由吗？ | 「手随之入画」没说为什么（改成「撑上膝盖」） |
| 5 | **时间尺度**：块时长内的环境变化真的可见吗？云影/日落/潮位是分钟级，秒级镜头写了等于白写 | 「山脊在薄云下缓慢变亮变暗」（6 秒内不可见） |

**为什么不做成正则**：五问全在语义层，正则写不窄——硬写就是假阳性工厂（负向词门禁扩词到 9 个、12 处全假阳的教训）。
**机制落点改在日志**：装配日志条目必须带 `"逻辑自查"` 字段，值为 `"过5问"` 或列出发现并修掉的问题。**没有这个字段的条目视同没自查。**

### 门禁：成品块出完必跑

```bash
python3 <skill>/tools/check_shots.py 块.txt      # 或 pbpaste | … -
```

七道：①参考图行 ②全局头三件套 ③镜号连续 ④四括号齐全有序 ⑤情绪打分 ⑥整块 4–30 秒 ⑦动作正文负向词 0。

⚠️ **两条判据是踩出来的**（2026-08-20，用 PM 标准范例自证时暴露）：
- **4–30 秒管的是整块，不是 cNN 单镜**。`c02,3s` 完全合法——块内镜头分段可以短于 4 秒。
- **护栏句豁免要用正则不能写死白名单**：写死会漏「不**能**切换外部机位」这种变体。

反向自证：造六种错（缺参考图／镜号跳号／括号乱序／情绪没打分／正文真负向词／整块超时）**全部抓到**，PM 原文**零误报**。

### 写正文时挂的知识库（写块前 Read `_索引/v6装配.md`）

| 写到哪 | 挂什么 |
|---|---|
| 情绪与动作 | BODY-35 换个性格要不要改 · BODY-29 藏就要写哪里在漏 · FACE-11 不受意志控制的部分 |
| 拐点节奏 | FRAME-35 动势的拐点 · BODY-21 速度节奏是标点 |
| 光 | FRAME-53 暗部要留破口 · FACE-15 暗部里的脸靠眼睛一点光 · FRAME-47/48 投影与交界线 |
| 景别与内容匹配 | BODY-40 幅度按景别缩放（中景看不见瞳孔） |
| 构图 | FRAME-51 字母形状骨架 · FRAME-52 垂直三区块 |

| 🔴 **平台负向框写什么** | `_负面提示词库.md`（本机）/ references 同名 | A人物一致 B肢体畸变 C表演分寸 D画质 E场景专用；**只进负向框不进正文**；POV/自拍镜先过冲突表 |

⚠️ **援引卡号写进装配日志，不进成品框。**

<details>
<summary>历史沿革（旧编号对照，点开看）</summary>

## ⭐ 关键帧格式（旧称 v6）· 成品块（2026-08-20 定稿 —— **每次沟通输出的就是它**）

**一段剧情 ＝ 一个可直接复制的纯文本框；多段剧情 ＝ 多个框。窗口显示，不落文件。**

框内固定三层：

```
[第1行 · 参考图]（只列本段用到的；图生视频时首帧图也挂这行）
场景={{Image 1}} 女孩={{Image 女孩参考图}} 男孩={{Image 男孩参考图}} 道具={{Image X}}

[第2段 · 全局头 · 一个自然段]
真人电影风格,画面不要出现任何文字，生成视频无BGM；16:9，<视角＋机位＋焦距声明>。
场景严格参考{{Image 1}}：<空间布局逐项枚举>。<风格／色调／胶片／光>。
<本段专属约束：人物一致性／道具无标识／动作唯一性（如"只取一片药"）／构图裁切规则>。

[第3层 · 镜头串]
c01,Ns,(空间:…)(姿态:角色-动作概括)(位置:主体-画面位置; 多主体用分号隔开)(情绪:角色-情绪词 强度/10) <景别＋焦距＋机位延续声明（"同一机位连续拍摄"）>。<连续动作，拐点分明，写到部件级>。<台词内嵌>。
c02,…
最后一镜末尾：只保留<音效白名单>，不加入旁白或音乐。【不出现任何文字字幕】
```

### 写法约定（从定稿上下文逐条抽的）

| 项 | 约定 |
|---|---|
| 台词 | `角色用中文<情绪><语气>地说道<台词>`；画外的标「在画外」 |
| 心声 | 标「非画面内心独白」＋**嘴唇保持闭合、绝不产生口型** |
| 情绪 | 每镜打分 `情绪词 N/10` |
| 时长 | 写在镜号后 `c01,4s`；一段总时长＝各镜之和 |
| 动作衔接 | 跨镜同一动作用「与上一镜进行动作匹配／同一批药片」防重复执行 |
| 跳过规则 | 没有的内容整行跳过（v5 规则继续有效）——没台词就没有台词句 |
| 护栏负向句 | 「无BGM／画面不要出现文字／不得跌倒」这类**是格式固定件，负向词门禁对它们豁免**；动作正文仍以正向优先（音效用「只保留…」白名单式） |

### 🚪 装配日志（硬规矩 · 2026-08-20 立 · Stop hook 核查）

**无日志条目不出块。** 每输出一个 v6 成品块，立即追加一条到 `~/Desktop/11/_规则库/_装配日志.jsonl`
（无大库的机器降级记到本 skill 目录 `装配日志.jsonl`）：

```json
{"ts":"<ISO时间>","session":"<会话ID>","block":"<块ID/剧情一句>","cards":["FRAME-35"],
 "materials":["实战经验"],"skipped":["台词"],"gaps":["想引但库里没有的"],"逻辑自查":"过5问","实测":"待回看"}
```

四条配套：
1. **写块前 Read** `_索引/v6装配.md`——Stop hook 查的是工具调用记录，「我记得内容」不算
2. **援引卡号写日志，不进成品**；输出块时窗口附一行本次援引的卡号
3. **开工先报队列**：skill 启动第一句报「待回看 N 条」（`grep -c 待回看 _装配日志.jsonl`）
4. **贴图即回看**：用户贴生成结果 → 认领日志条目 → 填实测 → 回写卡的 `实测:` 字段

核查器：`tools/v6_log_check.py`（Stop hook · 观测模式只记不拦，违规在 `_装配日志_违规.jsonl`；连续一周零误报后升拦截）。

## 内容自查清单（旧称 v5）· 字段序 → **降级为内容自查清单**

> v6 输出时正文必须**覆盖**这些字段的内容（主体/动作+情绪/一直在动的/声音），但排版不再按字段列表，按 v6 成品块。


**每一条视频提示词按下面的字段顺序一次生成；某字段当下没有上游描述就整行跳过——跳过，不发明（元规则 1）。**

```
━━━━━━━━━━ 全局资产（整个项目粘一次）━━━━━━━━━━

场景 = {{Image 1}} {{Image 2}}
她   = {{Image 3}} {{Image 4}}
他   = {{Image 5}} {{Image 6}}
人物站位 = {{Image 7}}

*无背景音乐，仅保留音效及台词

整体风格：<导演/影调锚，一句>
画面质感：<胶片型号 ＋ 饱和度 ＋ 色调 ＋ 对比度 ＋ 光的性格 ＋ 景深倾向>
氛围：<三到五个词>
运镜总则：<全片的手持/固定倾向，一句>

━━━━━━━━━━ 逐镜（每个关键帧一段）━━━━━━━━━━

#### 镜号 N：<景别>（<缩写>）—— <这一镜的戏核，一短句>

- 景别：<景别>，<画幅>。
- 机位/运镜：<构图名 ＋ 机位高度 ＋ 静止或怎么动>。
- 画面内容与空间指令：
  - 【主体】<谁，在画面哪个位置，朝向>
  - 【动作+情绪】<拐点前 → 拐点 → 拐点后；情绪写部件不写形容词>
  - 【一直在动的】<风吹的衣角/发丝这类，至少一项>
- 声音/台词：
  - 【音效】<...>
  - 【台词】<角色（口音，语气）："..."＞
- AIGC 控制要点：
  - <画幅> <景别英文>, <主体+状态英文>, <光线>, <镜头参数 f/x>, <胶片质感>
```

### 字段与既有纪律的对应（写的时候逐条核）

| 字段 | 挂的规矩 |
|---|---|
| 戏核一短句 | FRAME-42 先定戏核再定画面 |
| 【动作+情绪】拐点三段 | FRAME-35 动势的拐点；**情绪只写部件**（BODY-35：换个人这段要不要改） |
| 【一直在动的】≥1 项 | 运动元素清单精简版——治「一切静止」把画面冻成照片 |
| `*无背景音乐` | 括号缺席法的全局声明版 |
| 台词 | 落到 Seedance 成品串时转四括号 `{ }`，口音/语气放台词前括注 |
| 全部字段 | 🔴 负向词门禁（不要/不许/不得/没有/禁止 ＝ 0） |
| AIGC 控制要点 | 英文控制串，序：画幅→景别→主体状态→光→镜头参数→胶片质感；生图平台（Seedream/GPT Image）直接用 |

### 与 v4 的关系

- **v5（本节）＝ 关键帧工作流的标准格式**：先生关键帧图 → 图生视频，逐镜一段
- v4（五栏＋成品控制词）继续用于**文生视频长块**（《纪念日》v7 那类多镜块）
- ⚠️ 门禁脚本 `check_gates.py` 认的是 v4 块格式；v5 的可机检子集（负向词／偷懒面部／一直在动的）先用 grep 手跑，机检待做


<details>
<summary>⭐ 长块格式（旧称 v4 · PM 说的「v7 格式」就是它）—— 点开看完整定义</summary>

**提示词表有两个读者：导演（要审）和模型（要吃）。一份文件同时服务两者。**

```
═══ 全局资产（一次定义，N 块共享）═══
参考图绑定 · 音频总则 · 整体风格／画面质感／氛围／运镜总则 · 轨道表 · 禁令的正向表达 · 平台参数

═══ 逐块 ═══
块号 ｜ 场次 ｜ 时长 ｜ 轨道
  景别
  构图/机位          ← 含角度·距离·高度的数字
  空间               ← 指向场景卡的哪个点位
  角色状态与空间指令   ← ⭐ 按人分行
  光影控制            ← 含方位角·档数·投影几何·交界软硬
  `依据：FRAME-XX`   ← ⭐ 书证挂在光影栏正下方，或显式标「工程判断」
  ⭐ 时间轴          （表格）
  ⭐ 音频
  ⭐ 依赖与归属       （上游／下游／切点／生成批次）

  ### 精准控制词（可直接粘贴）
  ```…```
  校验点
```

### 为什么必须分栏

🔴 **实证（2026-08-14《纪念日》）**：一份混在自然语言里的提示词表，
**连续五次「整栏没搬」**——运镜／场景／站位／音频／光，每一次都是事后靠脚本抓出来的。

⭐ **分栏之后，空栏就是空栏，一眼看见。**
门禁也从「正则找漏」变成「查表格有没有空格」。

⚠️ **规则性留空要写明理由**（例：DV 轨道的「器材」栏留空——小传感器定焦，物理上没有浅景深）。
**规则留空和忘了填，在结构上必须是两回事。**

### 三样别人的格式里没有、多块叙事片必须有

| | 为什么 |
|---|---|
| **依赖与生成顺序** | 独立单镜没有依赖；**N 块叙事片有**。上游块不先验收就跑下游 ＝ 白跑 |
| **后期归属** | N 条独立视频要接成一部片，块间缝与跨块声音**平台做不到** |
| **书证标注** | 每条构图／光影判断标出依据哪张卡，或显式标「工程判断」 |

### 改格式时的硬约束

🔴 **改排版不许动成品控制词。** 改完必须逐块比对：

```python
assert blocks(old) == blocks(new), "控制词被改动了"
```

> **理由**：分栏是给人看的，控制词是给模型吃的。**动了后者，就不是换格式，是换内容。**

---

</details>


</details>

## 装配模板

### 模板 A · 官方语法（时间轴 ＋ 四括号）

```
对白语言：<语种>。

【时间轴】

0-X秒：<视觉风格>。<场景环境>。<主体 ＋ 动作/事件（部件级）>。<镜头运动>。
<环境音效>
<角色><语气>说：{台词}

X-Y秒：……

--ratio <画幅>
```

### 模板 B · 自由文本（包装层不认时）

```
<视觉风格>，<画幅>。<场景环境>。
<主体 ＋ 动作/事件（部件级）>。<镜头运动>。<光位>。
台词：<内容>
```

⚠️ **模板 B 下时长与画幅走界面控件，行内参数无效。**

---

## 常见错（官方点名 ＋ 实测）

| # | 错 | 后果 |
|---|---|---|
| 1 | **把视频写成了照片** | 只描述静止画面，没写这段时间里什么在变 |
| 2 | 参考素材没有 `@` 标注 | 素材无归属 |
| 3 | 标注写得太宽 | 连不想要的背景一起拉进来 |
| 4 | **一个节拍里堆多个运镜** | 全乱 |
| 5 | 把多场景故事塞进一段话 | 同上 |
| 6 | **用空词**（「电影感」「高级感」「氛围感」） | 官方原话：**「电影感」不是一个运镜，它什么也没告诉模型** |
| 7 | **用情绪词**（「她很伤心」） | G0 描述。**要写部件级状态**，情绪由观众读出来 |
| 8 | **基调压缩词进了提示词**（「像木头」） | 那是形体卡的内部记法，**只写由它推导出的部件描述** |
| 9 | 参考视频已给了运动，文字再重描一遍 | 打架 |
| 10 | **静止清单全量下发** | ⭐ **蜡像脸**（见 §1.3） |

---

## 与上游的边界

| 归分镜层（storyboard） | 归本层 |
|---|---|
| 景别 · 机位 · 焦距 · 主体位置 · 光位 · 视觉中心 · 轴线 · 眼睛高度 · 元素职务 · 静止清单 · 张力档 · **切点类型** · **块划分** | 把上述**翻译**成平台文本 · 措辞选择 · 括号语法 · 时间轴分段 · 平台参数 |

🔴 **提示词里出现分镜表没有的构图判断 ＝ 回写分镜表，不是就地发明。**
